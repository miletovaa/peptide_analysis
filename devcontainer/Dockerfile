# Base image for lab development container
# Replace 'ubuntu:22.04' with another base if needed (e.g., 'mcr.microsoft.com/devcontainers/python')

FROM ubuntu:22.04

# [Optional] Install dependencies (currently left empty for manual setup)
# RUN apt-get update && apt-get install -y python3 python3-pip

# [Optional] Set default shell
# SHELL ["/bin/bash", "-c"]

# [Optional] Set environment variables
# ENV PATH="/opt/conda/bin:${PATH}"

# [Optional] Command to run on container start
# CMD ["bash"]

# This is a minimal placeholder. Extend as needed for project-specific configurations.