{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Export Conda Environment",
      "type": "shell",
      "command": "conda env export --from-history > env/environment.yml",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    }
  ]
}